#!/usr/bin/env bash

SOLR_URL="http://localhost:8983/solr/products/select"

# CONSULTA 1 — BÚSQUEDA POR TEXTO
#
# Tomá el texto asignado en el formulario y construí aquí una consulta GET a
# /select. Debe buscar en title y description usando eDisMax y responder JSON.
#
# Antes de escribirla, decidí qué valores corresponden a q, defType, qf y wt.
# Después ejecutá el archivo, revisá numFound, ids, títulos, orden y score, y
# pegá el comando junto con la salida completa en el formulario.
